[package]
name = "af_token"
version = "0.1.0"
edition = "2021"
authors = ["AgentForge"]

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "21.7.7", features = ["alloc"] }

[dev-dependencies]
soroban-sdk = { version = "21.7.7", features = ["testutils"] }
